import { InjectionToken } from '@angular/core';
import { LogicManager } from '@olmi/common';

export const SUDOKU_PAGE_GENERATOR_LOGIC = new InjectionToken<LogicManager>('SUDOKU_PAGE_GENERATOR_LOGIC');
