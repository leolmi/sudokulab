import { Sudoku } from './lib/sudoku';
// import { repeat as _repeat } from 'lodash';


// export const check = (sdk: Sudoku): void => {
//   sdk.values = sdk.values || _repeat('0', sdk.rank * sdk.rank);
//   sdk.fixed = sdk.values || _repeat('0', sdk.rank * sdk.rank);
//   sdk.id = sdk.fixed;
// }
